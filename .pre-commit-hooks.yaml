-   id: clang-format
    name: Format C# and HLSL
    description: Format files with ClangFormat.
    entry: clang-format -i
    language: system
    files: \.(cs|hlsl|compute)$
    args: ['-fallback-style=none']
